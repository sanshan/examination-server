/*const dbs = require('mongodb')

const Schema = dbs.Schema

const menuItemSchema = new Schema({
    title: String,
    description: String,
    parent: String,
    menuId: String
})

module.exports = dbs.model('MenuItem', menuItemSchema, 'menu-items')*/
